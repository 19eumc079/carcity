part of styles;

class BoxBorders {
  static final BorderRadius primaryBoxBorders = BorderRadius.only(
    topLeft: Radius.circular(20),
    topRight: Radius.circular(20),
  );
  static final BorderRadius secondaryBoxBorders = BorderRadius.circular(25);
}
